// Arrow Function with Two Arguments
// Create an arrow function that takes two arguments and returns true if both numbers are even, otherwise it returns false.

// Define the Function

// Write an arrow function that checks if both numbers are even.
// Test the Function:
// Call the function with the numbers 4 and 6 and check the output.
// Call the function with the numbers 3 and 8 and check the output.

const isEven=(a,b)=>a%2==0&&b%2==0?true:false;

console.log(isEven(4,6))
console.log(isEven(3,8))

