// Write a JavaScript program that demonstrates the use of the following array methods: push(), shift(), unshift(), and pop().
// Step 1: Create an empty array
// Step 2: Use push()
// Add three elements to the end of the array using the push() method
// Print the array after each push operation
// Step 3: Use shift()
// Remove the first element of the array using the shift() method
// Print the array after the shift operation
// Step 4: Use unshift()
// Add two new elements to the beginning of the array using the unshift() method
// Print the array after each unshift operation
// Step 5: Use pop()
// Remove the last element of the array using the pop() method
// Print the array after the pop operation

let arr=[];
arr.push('I','Am','Shubham');
console.log(arr);
arr.shift();
console.log(arr);
arr.unshift('I',"Have");
console.log(arr);
arr.pop();
console.log(arr);