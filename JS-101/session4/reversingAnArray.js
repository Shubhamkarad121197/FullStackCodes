
// Define an array named arr with several repeated elements.
// Example: ['a', 'b', 'c', 'a', 'd', 'e', 'c', 'f', 'c'].
// Reverse the array using the .reverse() method and print the reversed array.
// Print the original array after using the .reverse() method to observe the effect on the original array.
let arr = ['a','b','c','a','d','e','c','f','c'];
console.log(arr.reverse());
console.log(arr);
